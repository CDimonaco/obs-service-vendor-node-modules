<service name="obs-service-vendor_node_modules">
  <summary>OBS Source Service to vendor node_modules</summary>
  <description>This Source Service will install node dependencies and vendor the node_modules folder in a compress archive. The archive could then be unpacked to have all the dependencies available in the build phase.</description>
  <parameter name="srcarchive">
    <description>Specify source archive name, required"</description>
  </parameter>
    <parameter name="subdir">
    <description>Working directory in which run all the install/compress procedure relative to the root of the source, not required.</description>
  </parameter>
  <parameter name="compression">
    <description>Compression algorithm of source archive, the same will be used to compress the node_modules dependencies</description>
  </parameter>
  <parameter name="vendor-archive-name">
    <description>Vendor archive name, will be compressed with the algorithm of source archive, default: node_vendor</description>
  </parameter>
  <parameter name="outdir">
    <description>Specify the output directory of the dependencies archive, required and provided by osc itself.</description>
  </parameter>
</service>